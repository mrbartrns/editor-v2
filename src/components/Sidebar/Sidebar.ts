import { Component } from "..";

interface Node<T> {
  nodeId: string;
  data: T | null
  children?: Node<T> | null
}

interface State {
  root: 
}

class Sidebar extends Component {
  setup() {}
}

export default Sidebar;
